[build]
  publish = "."                  # serve files from the repo root
  functions = "netlify/functions"

# (Optional) allow clean local dev URLs for functions
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
